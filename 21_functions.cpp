//function : block of code which runs when it is called
/* returntype funname(
{
   //do some work
   return some value(optional)
   }
   funname(); // calling function
*/
// declaration : function name,return type,parameter 
//defination ; declaration + body
//parameter:  variable that can have different values in function call
//arguments : fixed values on which function is performed
//current running function will be in the top of the stack
//scope:local(inside fun) vs global(outside fun) scope
//scope: is a area where variables are accessible

