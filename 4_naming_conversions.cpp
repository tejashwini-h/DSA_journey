// name should start witn an underscore or a letter
// can contain letters(uppercase and lowercase), digits, underscores
//case sensitive, no space,no special characters, no keywords
#include<iostream>
using namespace std;
int main()
{
    int a=5, b=10;// valid 
   // int return = 12;//invlid as return is a keyword;
   }

